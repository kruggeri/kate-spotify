import DS from 'ember-data';
// import attr from 'ember-data/attr';

export default DS.Model.extend({

  // TODO: I'll probably want to add a track model once I get to playlists

  // name: attr(),
  // audioSource: attr(),
  // album: DS.belongsTo('album')
});
